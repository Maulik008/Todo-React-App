const iState = {
    name: "mihir",
    lastName: 'abc',
    wishes: ['eat', 'code']
}


const reducer = (state = iState, action) => {
    //console.log(action);
    return state;
}
export default reducer;